language: node_js
node_js:
  - 10
cache:
  directories:
  - node_modules
before_install:
  - npm i -g firebase-tools
after_success:
  - npm run release
  - firebase deploy -m "Travis deploy" --non-interactive --token $FIREBASE_TOKEN
branches:
  only:
    - master